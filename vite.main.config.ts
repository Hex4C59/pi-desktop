import { defineConfig } from 'vite';

// Bundle app code and production dependencies into the main process output.
// The Forge Vite plugin packages only `.vite/` (see @electron-forge/plugin-vite).
export default defineConfig({});
